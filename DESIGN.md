# 项目技术选型与采集架构

更新日期：2026-09-22

## 1. 结论

项目继续使用 Python、FastAPI、SQLAlchemy 和原生前端。

采集器从 Playwright 改为 `httpx` 纯 HTTP 客户端。

这次选型不重写业务层。原项目的主要资源问题来自 Chromium，不是 Python。

## 2. 已验证的 eWeb 私有 API

目标固件使用以下接口：

```text
GET  /cgi-bin/luci/
POST /cgi-bin/luci/api/auth
POST /cgi-bin/luci/api/cmd?auth={sid}
```

登录流程如下：

1. 读取登录页。
2. 从登录页提取当前 AES 口令。
3. 使用 OpenSSL 兼容的 AES-256-CBC 加密密码。
4. 调用 `api/auth`。
5. 保存响应中的 `token`、`sid` 和 Cookie。
6. API 查询参数使用 `sid`，不是 `token`。

命令请求需要两个签名头：

```text
Content-Accept  = MD5("Web@Rj$2020!" + UTF-8请求字节数)
Contents-Accept = MD5("Web@Rj$2020!" + 紧凑JSON请求正文)
```

已在当前设备上验证以下模块：

```text
local_topology
user_list
```

验证结果为 HTTP 200 和业务 `code=0`。终端接口一次返回 66 条数据。

## 3. 新采集链路

```text
FastAPI lifespan
  -> RuijieCollectorSupervisor
    -> EwebApiClient
      -> 登录页密钥发现
      -> AES 登录
      -> HTTP Keep-Alive
      -> 并发请求 topology 和 user_list
    -> 现有解析器
    -> 现有状态和历史处理
    -> SQLite 或 PostgreSQL
```

采集周期不再启动浏览器，也不再点击页面。

登录完成后，每轮采集只发送两个并发 HTTP 请求。

认证失效时，客户端重新读取密钥并登录一次。

## 4. 技术选型

### 后端

保留 FastAPI。

系统以网络等待和数据库写入为主。异步 Python 足以处理单台路由器。

改用 `httpx.AsyncClient`。它支持连接复用、超时、Cookie 和并发请求。

使用 PyCryptodome 实现 AES。不要维护自制 AES 代码。

### 数据库

默认保留 SQLite。

单实例和单管理员场景不需要 PostgreSQL。SQLite 的部署成本最低。

多实例、远程数据库或大量历史数据场景再使用 PostgreSQL。

### 前端

保留原生 HTML、CSS 和 JavaScript。

当前前端没有构建依赖，运行开销小。暂时改成 React 或 Vue 不会提高采集效率。

后续只在需要组件化和 TypeScript 时引入 Vite。

### 容器

使用 `python:3.12-slim`。

删除 Playwright 和 Chromium 安装步骤。

这会减少镜像体积、启动时间、内存占用和共享内存要求。

## 5. 兼容策略

私有 API 可能随固件变化。

客户端动态读取登录页 AES 口令，不把口令写死在代码中。

命令结构集中在 `backend/collector/eweb_api.py`。

如果新固件改变接口，应新增固件适配器。不要恢复常驻浏览器。

仅在一次性分析未知固件时使用浏览器抓包工具。

## 6. 安全边界

只对用户管理的局域网设备使用私有 API。

不要记录密码、AES 密钥、SID、Token 或完整 Cookie。

`scratch` 中的抓包文件和测试脚本不能进入生产镜像。

生产请求只读取状态。不要调用配置修改、重启或升级接口。

## 7. 客户端路径和推断

AP 或交换机的别名作为业务位置名称，例如“3楼办公室”或“车间东区”。

客户端路径由在线会话、漫游段和离线间隔合并生成。

推断规则保持保守：

- 30 秒内掉线并回到同一接入点：较可能是信号抖动或短时遮挡。
- 2 分钟内回到同一接入点：可能是休眠、短暂离开覆盖区或信号波动。
- 10 分钟内在另一接入点出现：可能正在区域之间移动。
- 长时间没有连接任何接入点：可能离开公司网络覆盖范围。

系统不能根据网络数据判断上厕所、开会或其他具体行为。界面必须显示置信度和推断免责声明。

## 8. 采样和留存

```text
常规客户端：约 60 秒采样，保留 1 天
关注客户端：约 10 秒采样，保留 30 天
```

曲线接口按时间范围聚合为最多数百个点，避免浏览器绘制过多数据。
## 9. 客户端资料清理

客户端身份资料不是按 1 天或 30 天删除。

只有在客户端离线，并且连续 365 天没有再次出现在采集快照中时，才会清理：

- 客户端记录；
- 自定义备注和别名；
- 流量与 RSSI 样本；
- 上下线事件；
- 会话记录；
- AP 漫游段。

在线客户端不会被清理。最近重新出现的客户端不会被清理。关注状态不会绕过一年无活动清理规则。

## 10. 关注信息与离线确认

首页在运行状态下方显示关注客户端的确认状态。

```text
确认在线：客户端仍在采集结果中，显示当前位置。
位置已变化：显示“从 A 切换到 B”。
确认离开：连续超过 60 秒不在完整采集结果中。
```

少于或等于 60 秒的短时缺失不结束会话，不发送离线事件，也不标为离开。
