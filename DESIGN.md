# 项目实现思路与架构 (Architecture & Methodology)

这是一个以**“够用、稳定、高效”**为核心指导思想的微型监控项目。我们在技术选型和架构设计上，尽量避免过度工程化，力求简单务实。

## 1. 为什么选择 Playwright 模拟抓取？

在开发初期，我们尝试过直接分析锐捷 eWeb OS 的原生 API，但发现其存在比较繁琐的安全机制：
- 数据传输（指令和密码）由前端 JS 动态使用 AES-CBC 加密。
- 密钥和 IV 由服务器在登录时动态下发，并且和会话高度绑定。
- 请求头带有基于时间戳和 Cookie 的复杂防伪签名。

**我们的权衡：**
如果我们去硬破解并用 Python 重写这些加密逻辑，虽然能实现，但代码会变得非常复杂。一旦未来路由器更新了固件，稍微改动了一下加密算法，我们的代码就会直接失效，维护成本极高。

**务实的解法：**
作为个人微项目，**稳定性**是最重要的。因此我们选择在后端跑一个基于 `playwright` 的无头浏览器实例：
1. 我们让后台隐式地打开一个真实浏览器，加载官方的原版页面。
2. 借用官方自己原封不动的前端代码去完成复杂的加密、签名和会话维持。
3. 我们只需在浏览器内存中，拦截并解析已经被解密成明文的局域网设备数据（如 `local_topology`）。

这种方法也许不够“高端”，但它极其稳固。无论官方怎么更新加密外壳，只要内部数据结构没大变，我们的系统就永远能跑。

## 2. 极简的系统架构

为了做到轻量高效，系统只包含了最核心的组件：

- **数据获取 (Collector)**：后台每隔几秒，通过无头浏览器偷偷“看一眼”路由器的实时状况。
- **状态机与对比引擎**：将刚刚看一眼拿到的数据，与上一秒的数据进行比对。如果发现设备换了位置，就产生一条“漫游”记录；如果发现设备不见了，就生成一条“下线”记录。
- **数据持久化 (ORM)**：通过 SQLAlchemy 原生支持 SQLite，同时只要在前端改一下配置，它也能把数据塞进性能更好、不怕文件损坏的 PostgreSQL 库里。
- **极简前端**：没有引入 React/Vue 这些大框架，就是一个纯静态的 `index.html` 配合原生 JS。打开就能用，足够直观地把后端的历史数据和流量趋势画出来即可。

## 3. 数据库结构简述

系统把精力集中在记录设备行为上，核心的业务表是 `ConnectionHistory`（连接历史）：

- `mac`: 设备的 MAC 地址。
- `ap_name`: 连在了家里的哪个路由器节点上（比如主卧、客厅）。
- `session_start` / `session_end`: 记录了设备什么时候来的，什么时候走的。
- `total_rx_bytes` / `total_tx_bytes`: 统计这段时间里，它跑了多少流量。

通过这些基础的字段积攒下的数据，我们就可以轻松追溯家中任何智能设备或手机的网络连接质量与历史轨迹了。
